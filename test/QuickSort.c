#include<stdio.h>
#include<math.h>
#include<ctype.h>
#include<string.h>
#include<stdlib.h>
#include<time.h>

#define INF 1010
#define swap(A, B) { int temp = A; A = B; B = temp; }

int main() {
    double num = 123.723;
    int x = (int)num;
    printf("%d", x);
    
}
